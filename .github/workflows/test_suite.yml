name: Test Suite

on:
  push:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          # Rails 6.1 — Ruby >= 2.5, our floor is 3.1
          - rails: "6.1"
            ruby: "3.1"
            gemfile: gemfiles/rails_6_1.gemfile
          # Rails 7.0 — Ruby >= 2.7, our floor is 3.1
          - rails: "7.0"
            ruby: "3.1"
            gemfile: gemfiles/rails_7_0.gemfile
          # Rails 7.1 — Ruby >= 2.7.3
          - rails: "7.1"
            ruby: "3.1"
            gemfile: gemfiles/rails_7_1.gemfile
          - rails: "7.1"
            ruby: "3.3"
            gemfile: gemfiles/rails_7_1.gemfile
          # Rails 7.2 — Ruby >= 3.1
          - rails: "7.2"
            ruby: "3.1"
            gemfile: gemfiles/rails_7_2.gemfile
          - rails: "7.2"
            ruby: "3.3"
            gemfile: gemfiles/rails_7_2.gemfile
          - rails: "7.2"
            ruby: "4.0"
            gemfile: gemfiles/rails_7_2.gemfile
          # Rails 8.0 — Ruby >= 3.2
          - rails: "8.0"
            ruby: "3.2"
            gemfile: gemfiles/rails_8_0.gemfile
          - rails: "8.0"
            ruby: "3.4"
            gemfile: gemfiles/rails_8_0.gemfile
          - rails: "8.0"
            ruby: "4.0"
            gemfile: gemfiles/rails_8_0.gemfile
          # Rails 8.1 — Ruby >= 3.2
          - rails: "8.1"
            ruby: "3.2"
            gemfile: gemfiles/rails_8_1.gemfile
          - rails: "8.1"
            ruby: "3.4"
            gemfile: gemfiles/rails_8_1.gemfile
          - rails: "8.1"
            ruby: "4.0"
            gemfile: gemfiles/rails_8_1.gemfile

    name: Rails ${{ matrix.rails }} / Ruby ${{ matrix.ruby }}

    env:
      BUNDLE_GEMFILE: ${{ github.workspace }}/${{ matrix.gemfile }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true

      - name: Run tests
        run: bundle exec rspec
